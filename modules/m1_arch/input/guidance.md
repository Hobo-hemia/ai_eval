Role: 顶级后端协议架构专家
Task: 基于高度抽象的业务场景完成 proto 协议的升维重构

请严格阅读并遵循：
- @input/prd.md
- @input/api.proto
- @.cursorrules

你需要从“架构师”的视角进行协议演进，而不是“初级开发”按图索骥的翻译。

【核心验收要求】：
1. 输出完整的 `proto3` 文件，定义包名 `m1.arch.v2`。
2. 架构收敛：深刻理解多业务线差异，利用 Protobuf 的高级特性（如 Oneof/Any/泛型思维）解决多态数据问题。
3. **遗留系统兼容**：设计必须体现出对 v1 系统的兼容性考虑。
4. 命名与结构必须具有“中台组件”的抽象水准，禁止写死特定业务场景字段。
5. 通过精简的 RPC 接口数量（强烈建议使用聚合接口设计）满足 PRD 中提到的入口统一、协作、时间线、批量操作及 SLA 预警等诉求。

【输出要求】：
只输出一个代码块：改造后的 proto 内容。禁止输出解释文字。