Role: 后端协议架构专家
Task: 基于抽象产品需求完成 proto 协议升级

请严格阅读并遵循：
- @input/prd.md
- @input/api.proto
- @.cursorrules

你需要从“后端专家视角”做协议重构，不要按产品功能点逐条堆接口。

【核心验收要求】：
1. 输出必须是完整 `proto3` 文件，package 升级到 `m1.arch.v2`。
2. 必须体现对 v1 的改造：接口重命名、字段新增/删除、语义收敛。
3. 新增 rpc 数量必须 `< 5`，并能覆盖 PRD 的 5 个功能点。
4. 协议中应包含可支持时间线、协作人、批量操作、SLA 风险提示的数据结构。
5. 命名要抽象、可复用，避免写死某一个业务线。

【输出要求】：
只输出一个代码块：改造后的 proto 内容。禁止输出解释文字。
